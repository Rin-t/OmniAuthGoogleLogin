// このファイルをコピーして OAuthSecrets.swift を作成し、
// bin/rails runner "app = Doorkeeper::Application.first; puts app.uid; puts app.secret"
// で出力される値を書き込む。
// OAuthSecrets.swift は .gitignore 対象なのでコミットされない。

import Foundation

enum OAuthSecrets {
    static let clientID     = "YOUR_DOORKEEPER_APPLICATION_UID"
    static let clientSecret = "YOUR_DOORKEEPER_APPLICATION_SECRET"
}
